version: '3.8'

# Docker Compose for auxiliary services only
# Backend and MySQL run locally for faster development

services:
  # Redis - Uncomment when needed for caching/sessions
  # redis:
  #   image: redis:7-alpine
  #   container_name: swd_redis
  #   restart: unless-stopped
  #   ports:
  #     - "6379:6379"
  #   networks:
  #     - swd_network

networks:
  swd_network:
    driver: bridge
